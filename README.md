Follow these steps to get the project up and running on your local machine:

1. Clone the repository

2. Configure the connection string
   
   App is currently using default connection string, so you need to configure it if needed. 

3. Apply migrations
   
   Run the " Update - Database " command via Package Manager Console

4. Run the program
   
   You can test the app directly in the browser via Swagger Interface.
